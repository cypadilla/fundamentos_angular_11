const username: string | number = 'hla';
const sum = (a: number, b:number) => {
    return a+b;
}
sum(1,1);

class Person {
    constructor(public age:number, public lastName:string){}
}

const cristian = new Person(20,'padilla');

